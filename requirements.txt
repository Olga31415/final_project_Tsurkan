# Минимальные версии; верхние границы не заданы, чтобы pip мог взять готовые wheel
# (например, для новых релизов Python без локальной сборки Rust/MSVC).
fastapi>=0.115.6
uvicorn[standard]>=0.32.1
pydantic>=2.11.0
httpx>=0.28.1
pytest>=8.3.4
